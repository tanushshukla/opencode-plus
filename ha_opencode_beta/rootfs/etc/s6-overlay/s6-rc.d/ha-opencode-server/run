#!/command/with-contenv bashio
# This service must not spawn a second OpenCode server. Authenticated V2 LAN
# attachment is a separate integration task against the server on port 4100.
if bashio::config.true 'enable_server'; then
    bashio::log.warning "V2 LAN authentication setup is pending; the private conversation server remains on loopback."
fi
exec sleep infinity
